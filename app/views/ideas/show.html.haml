.header-and-tags-line
  %h1= @idea.title
  = render_tags @idea.tags, true

%p
  State:
  %strong= @idea.state.title

%p= @idea.target

- if @idea.description.present?
  %hr
  = @idea.description

-if @features.present?
  = render :partial => "shared/feature_list"